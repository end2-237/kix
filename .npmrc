# npm installe les peer dependencies optionnelles par défaut : drizzle-orm en
# déclare une sur better-sqlite3, qui exige node-gyp + Python à la compilation.
# On les omet — nos dépendances réelles sont toutes déclarées explicitement.
omit=peer
