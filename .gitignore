design/build/
design/preview/
design/*.html
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Certificat auto-signé que MediaMTX dépose dans le répertoire courant quand on
# le lance depuis la racine du dépôt (serveur MoQ). C'est une clé privée : elle
# n'a rien à faire ici, même jetable.
auto.crt
auto.key
